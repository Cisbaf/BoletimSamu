# Imagem de DESENVOLVIMENTO do frontend: Vite dev server com HMR.
# Não usar em produção (o build de produção acontece dentro do Dockerfile raiz).
FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
